import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-tabs-navigation',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
